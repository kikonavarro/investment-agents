# Datos financieros
yfinance>=0.2.40
yahooquery>=2.3.7

# Excel (excel_portfolio — cartera; excel_generator dormido también lo usaría)
openpyxl>=3.1.0

# Utilidades
pandas>=2.2.0
numpy>=1.26.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Tests
pytest>=8.0.0

# Dependencias de módulos DORMIDOS (scheduler, document_generator):
# ver requirements-optional.txt — instalar solo si se reactivan.
