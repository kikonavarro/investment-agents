"""
Thesis Writer — LEGACY.

La tesis ahora la escribe Claude Code vía la skill `thesis-writer`.
Este módulo mantiene run_thesis_writer() como stub para compatibilidad.
"""


def run_thesis_writer(ticker: str) -> str:
    """Stub — ya no llama a la API. Usar skill thesis-writer desde Claude Code."""
    print(f"  [thesis_writer] LEGACY: usar Claude Code + skill thesis-writer para {ticker}")
    return f"[LEGACY] Usa Claude Code para escribir la tesis de {ticker}"
